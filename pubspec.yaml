name: segment_display
description: Segment display widget for Flutter. Supports multiple types of segment displays and segment customization.
version: 0.4.2
homepage: https://github.com/janstol/flutter_segment_display

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^1.3.0
