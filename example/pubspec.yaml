name: example
description: Segment display example
version: 1.0.0+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.16.0
  segment_display:
    path: ../

flutter:
  uses-material-design: true
